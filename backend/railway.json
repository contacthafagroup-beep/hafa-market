{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node node_modules/.bin/prisma migrate deploy && node prisma/seed.js && node src/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
